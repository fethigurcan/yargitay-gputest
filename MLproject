name: yargitay-gputest

conda_env: conda.yaml

entry_points:
  main:
    #parameters:
    #  alpha: {type: float, default: 0.5}
    #  l1_ratio: {type: float, default: 0.1}
    command: "python3 -m torch.distributed.launch --nproc_per_node 4 run.py"
    # {alpha} {l1_ratio}"